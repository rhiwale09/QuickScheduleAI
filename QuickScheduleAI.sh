# Backend setup
cd Backend
#npm init -y
#npm install express cors multer pdf-parse tesseract.js chrono-node googleapis openai ics mammoth textract axios dotenv xlsx

# Create .env file and add your API keys

# Start backend
node server.js

# In a new terminal - Frontend setup
cd Frontend
#npx create-react-app .  # If not already done
#npm install axios

# Replace src/App.js with the clean version

# Start frontend
npm start
